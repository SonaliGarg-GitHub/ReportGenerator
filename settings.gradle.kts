pluginManagement {
	repositories {
		maven { url = uri("https://repo.spring.io/milestone") }
		maven { url = uri("https://repo.spring.io/snapshot") }
		maven { url = uri("https://repository.aspose.com/repo/") }
		gradlePluginPortal()
	}
}
rootProject.name = "ReportGenerator"
